Name: Delete Deployments

on:
  delete:
    branches-ignore:
      - main

  jobs:
    delete:
      runs-on: ubuntu-latest
      steps:
        - uses: strumwolf/delete-deployment-environment@releases/v1
          with:
            token: ${{ github.token }}
            environment: yakunosproject
